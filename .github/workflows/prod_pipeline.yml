#name: Dev to Prod
#testing

name: Deploy to Development

on:
  push:
    branches: [main]


jobs:
  Auto_Tag:
    name: Auto tag on Push
    runs-on: ubuntu-latest
    permissions:
      contents: write

    outputs:
      version: ${{ steps.tag.outputs.new_tag }}

    steps:
      - uses: actions/checkout@v4
        with:
            fetch-depth: 0
        
      - name: Bump Version and push tag
        id: tag
        uses: anothrNick/github-tag-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEFAULT_BUMP: patch
          WITH_V: true



  Build_and_Push:
    name: Build and Push Docker Image
    needs: Auto_Tag
    runs-on: ubuntu-latest

    outputs:
      image: ${{ steps.image.outputs.image }}
      version: ${{ needs.Auto_Tag.outputs.version }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Login into Digital Ocean
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: ${{ secrets.DIGITALOCEAN_TOKEN }}
          password: ${{ secrets.DIGITALOCEAN_TOKEN }}

      # - name: Assign version
      #   id: version_cal
      #   uses: paulhatch/semantic-version@v5.4.0
      #   with:
      #     tag_prefix: "v"
      #     version_format: "${major}.${minor}.${patch}"
          

      - name: Set Image Name
        id: image
        run: |
          IMAGE="${{ vars.REGISTRY }}/${{ vars.IMAGE_NAME }}"
          echo "image=$IMAGE" >> $GITHUB_OUTPUT
        
      - name: Build and Push Docker image
        run: |
          
          echo "Build and Push image to Digital Ocean"

          docker build -t ${{ steps.image.outputs.image}}:${{ needs.Auto_Tag.outputs.version }} .
          docker push ${{ steps.image.outputs.image}}:${{ needs.Auto_Tag.outputs.version }}

  Deploy_Development:
    name: Deploy Development
    runs-on: ubuntu-latest
    needs: Build_and_Push
    steps:

      - name: Login into Digital Ocean
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: ${{ secrets.DIGITALOCEAN_TOKEN }}
          password: ${{ secrets.DIGITALOCEAN_TOKEN }}

        
      - name: Fetch Image and deploy to Dev
        id: docker_step
        run: |
          
          IMAGE="${{ needs.Build_and_Push.outputs.image }}:${{ needs.Build_and_Push.outputs.version }}"

          echo "Pull Docker image from DO"
          docker pull $IMAGE

          echo "deploying to Development"
          docker rm -f myapp || true
          docker run -d --name myapp -p 8080:8080 $IMAGE

      - name: Success Message
        if: steps.docker_step.outcome == 'success'
        run: |

          echo "Application deployed Successfully!"

          IMAGE="${{ needs.Build_and_Push.outputs.image }}"
          VERSION="${{ needs.Build_and_Push.outputs.version }}"
          VERSIONED_IMAGE="${{ needs.Build_and_Push.outputs.image }}:${{ needs.Build_and_Push.outputs.version }}"

          #echo "Pull Docker image from DO"
          #docker pull $IMAGE:$VERSIONs
          docker pull $VERSIONED_IMAGE

          echo "Adding latest tag to the image"
          docker tag $VERSIONED_IMAGE "$IMAGE:latest"

          docker push "$IMAGE:latest"



          


          




      
        
        



#'tags': ['latest', '1.0.270']
#'tags': ['Prod', '1.0.270']

#   tagging:
#     runs-on: ubuntu-latest
#     needs: development
#     steps:
#       - name: Checkout Code
#         uses: actions@checkout@v4

#       - name: Prod Version Tagging
#         id: version_cal
#         uses: paulhatch/semantic-version@5.4.0
#         with:
#           tag_prefix: "Prod-v"

#       - name: Login into Digial Ocean Registry
#         uses: docker/login-action@v3
#         with:
#           registry: registry.digitalocean.com
#           username: ${{ secrets.DIGITALOCEAN_TOKEN }}
#           password: ${{ secrets.DIGITALOCEAN_TOKEN }}

#       - name: Pull, Retag and Push Image
#         run: |

#           SOURCE_IMAGE="${{ secrets.REGISTRY }}/${{ secrets.IMAGE_NAME }}:latest"
#           TARGET_IMAGE="${{ secrets.REGISTRY }}/${{ secrets.IMAGE_NAME }}:${{ steps.version_cal.outputs.version_tag }}"

#           echo "Pulling image from $SOURCE_IMAGE"
#           docker pull $SOURCE_IMAGE

#           echo "Retagging the image"
#           docker tag $SOURCE_IMAGE $TARGET_IMAGE







  